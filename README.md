# docker_images
Basic docker images

STEPS:-
1. Rename the files to 'Dockerfile' to use it without any worries.
$ mv <filename> Dockerfile

2. Built it
docker build -t <tag_if_u_want_to_give>:<version> .

